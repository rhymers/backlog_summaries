<%= form_for(created_user) do |f| %>
  <% if created_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(created_user.errors.count, "error") %> prohibited this created_user from being saved:</h2>

      <ul>
      <% created_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
